#include<stdio.h>
int main()
{
    int a=(int) 1.99999999;
    printf("%d", a);
    //compiler are force fully convert the float value into int value.[explicit type convertion].
    // we ,made a changes thats why its called as explicite type covertion.

}