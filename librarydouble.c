#include<stdio.h>
int main()
{           // library fuction.{{pow(), sqrt(), strcmp(), strcpy() etc}}    
    double var;
    var = 81;
    double squareroot = sqrt(var);
    printf("the square root of %2.1f is = %2.1f " , var , squareroot);

    return 0; 

}
// c languje in function are many types 
// its have 2 types 
//  1.library func.   2.user defined function.